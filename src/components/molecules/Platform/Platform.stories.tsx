import type { Meta, StoryObj } from '@storybook/react';

import Platform from '.';

export default {
  title: 'Molecules/Platform',
  component: Platform
} as Meta<typeof Platform>;

type Story = StoryObj<typeof Platform>;

export const Default: Story = {};
