import type { Meta, StoryObj } from '@storybook/react';

import Solutions from '.';

export default {
  title: 'Molecules/Solutions',
  component: Solutions
} as Meta<typeof Solutions>;

type Story = StoryObj<typeof Solutions>;

export const Default: Story = {};
